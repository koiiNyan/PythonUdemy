range_list = list(range(0,21))
print(range_list)

# range()  creates a Python range object.
# To get a real list object you need to use the list() function
# to convert the range object into a list object.
